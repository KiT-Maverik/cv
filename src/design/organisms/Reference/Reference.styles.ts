import {SxProps} from "@mui/material";
import {Theme} from "@mui/material/styles";

const container: SxProps<Theme> = {
    display: "flex",
    flexWrap: "wrap",
    gap: 1,
}

export default {
    container,
}
